{
  "name": "metasearch",
  "version": "1.1.0",
  "description": "A browser extension for enhanced web search capabilities across Chrome and Firefox",
  "scripts": {
    "build": "rspack build --mode=production",
    "package": "npm run build && node scripts/package.js"
  },
  "devDependencies": {
    "@rspack/cli": "^1.2.6",
    "adm-zip": "^0.5.10"
  }
}
